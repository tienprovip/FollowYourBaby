import { View, Text } from 'react-native';

export default function AIChatScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-brand-peach">
      <Text className="text-xl font-bold text-brand-navy">AI trợ lý</Text>
    </View>
  );
}
